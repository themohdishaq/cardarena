export const metadata = {
  title: "Finding Players",
  description: "Waiting for 4 players to join. Bazar Game matchmaking in progress — no login required.",
  robots: { index: false, follow: false },
};

export default function WaitingLayout({ children }) {
  return children;
}
